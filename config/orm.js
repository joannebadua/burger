var connection = require("../config/connection.js");
["?","?","?"].toString() => "?,?,?";
function printQuestionMarks(num) {
    var arr = [];
    for (var i = 0; i < num; i++) {
        arr.push("?");
    }
    return arr.toStrin();
}
function objToSql(ob) {
    var arr 
}